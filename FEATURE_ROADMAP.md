# FEATURE_ROADMAP.md — GRID 1.0 to launch

Approved build order. Status updated each task. Reveal is manual: waitlist ships first, full platform stays behind `PLATFORM_LIVE` until we flip it.

| Phase | Scope | Status | Blocked on |
|---|---|---|---|
| **0. Supabase foundation** | Provision Supabase (EU); migrate DB Turso→Postgres (Drizzle kept); Better Auth→Supabase Auth; RLS on every table; env/secrets | **COMPLETE ✓** — DB on Supabase Postgres; Supabase Auth (mirror trigger, guest=anonymous); **RLS on all 15 tables** (supabase/rls.sql) with isolation proven (scripts/test-rls.mjs: B can't read/update A, anon denied, server-only tables locked). App reads via Drizzle/`postgres` (bypasses RLS) + app-layer checks; RLS locks the publishable-key/PostgREST surface. | Live guest/signup needs 2 Supabase toggles (Anonymous ON, Confirm-email OFF). Prod deploy needs the IPv4 **pooler** string. |
| **1. Waitlist site (Phase A)** | Flawless standalone `/waitlist` on Supabase; Resend confirmation + admin notification; admin view + CSV; **`PLATFORM_LIVE`** gate | **Built ✓** — `joinWaitlist` writes to Supabase + fires confirmation + admin-notify emails (Resend wired, no-ops cleanly until keys); admin Launch section (PLATFORM_LIVE toggle, editable live via `app_config`) + waitlist list + CSV; gate redirects platform→/waitlist when off (admins pass, default ON). tsc+build green. | **Resend keys** to actually send (SETUP.md §2) |
| **2. Platform persistence + gating** | Real tables; single **server-side entitlements service**; DB-backed CMS/config; **50/5 GB** quotas; remove demo auth | **Foundation ✓** — server `entitlements.ts` (subscription→feature access, Stripe-webhook integration point), `subscription`/`usage`/`app_config` tables (RLS-locked), `config-store` CMS, `quota.ts` (50 GB total / 5 GB file, atomic). **Supabase Storage ✓** — private `uploads` bucket + owner-scoped storage RLS (`uploads_own`, files keyed `${uid}/…`); `storage.service.ts` (server upload reserves quota then rolls back on failure, signed up/download URLs) verified headless (upload/list/delete + atomic 50/5 GB guard blocks over-cap; throwaway user cascades clean). **Server entitlement enforcement ✓** — `requireFeatureAccess(userId,key)` gates the creator/owner write paths (shop: getMyShop/updateShopConfig/create/removeProduct; academy: getMy/updateConfig/create+removePath/Lesson; campaign API route) with AuthError(403); buyer/learner paths (purchase, enroll, browse) intentionally NOT gated. Mirrors the client nav-hide; inert while DEMO_MODE on, enforces tiers at launch. **Seed-data Rule 3 fix ✓** — SEED_PRODUCTS/SEED_PATHS insertion now gated on DEMO_MODE; at launch new shops/academies start empty (verified prod DB currently has 0 rows — nothing to wipe). **Domain tables ✓** — `contract`, `contract_version`, `review`, `dispute` (non-money only; amounts/escrow/refunds deferred to Phase 3 for a single money source) + RLS proven (party-scoped reads via EXISTS, public review read, cross-user denial, cascade cleanup — all PASS). **Product-file upload ✓** — real deliverable now uploads straight to the private bucket via a short-lived signed URL (owner-gated + size-checked), `createProduct` reserves quota atomically (rolls back the orphan on over-cap), buyer "Download files" hits a purchase-gated signed URL, `removeProduct` frees the object + quota. Look unchanged; no money logic touched. Verified headless (signed-URL issue → browser upload → gated download byte-match → cleanup). **Creator marketplace DB-backed ✓** — full creator-profile feature (profile-actions.ts): profile table expanded + `portfolio_item`/`creator_package` tables (RLS owner-only); editable profile with portfolio image uploads (signed URL → quota) + package builder + publish/availability toggles; browse/creative-detail/home-rail/radar/your-profile all read `listCreators`/`getCreator` with empty states; no static CREATIVES render in the marketplace anymore. Verified headless (published-listing + review agg 8/8; portfolio upload/quota/cascade E2E 5/5). Layout preserved; package prices are creator-listed (no charge logic). **Remaining for launch:** (a) secondary demo surfaces still using `findCreative` — community feed, saved, concierge — bound to other mock sets (POSTS/SAVED_CREATIVE_IDS/concierge plans); (b) client company profile (MY_COMPANY) still mock; (c) remove guest button + flip DEMO_MODE. (a)/(b) are separate de-mock efforts; nothing fake reaches the *marketplace*, but these surfaces must be DB-backed or demo-gated before going live. | decision (3) empty-states-at-launch OK |
| **3. Payments (Stripe-ready, connect LAST)** | Connect separate charges & transfers ("Grid Escrow" held funds, ≤90-day guard, dispute→reverse); Checkout/Billing subs; signature-verified webhook; payment tables; 6 integration points — all against **test mode** | Approved | Stripe account (SETUP.md §4); decisions (1)(2) prices/fees/windows — **money diff requires explicit yes** |
| **4. Audit + cleanup + docs** | Unit/integration/e2e tests for every critical flow; money path vs Stripe test mode; RLS tests (A can't read B); pass/fail report; dead-code/dep prune (**deletes pre-approved**); finish PROJECT_MAP/PAYMENTS/CHANGELOG | Approved | Phases 0–3 |

## Hard rules (never violate)
1. No money/Stripe/pricing/payout change without showing the diff and getting an explicit yes.
2. No UI/visual redesign. Backend + wiring only. Backend-forced visible change → ask first.
3. No mock/seed/demo data in any env that can reach production; test data lives only in isolated dev DB, wiped before deploy.

## Open decisions (from the plan)
1,2 (money: fees/prices/escrow windows) — needed at Phase 3. 3 (empty states at launch) — needed at Phase 2. 4 (Turso disposable) — needed at Phase 0. 5 (Resend) — recommended, awaiting confirm. 6 (file deletions) — gated, list-then-yes.
