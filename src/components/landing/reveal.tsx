"use client";

/**
 * Reveal is now powered by Framer Motion (see ./motion). Kept as a thin
 * re-export so existing section imports stay stable. Honors
 * prefers-reduced-motion via the <MotionConfig reducedMotion="user"> set in
 * landing.tsx.
 */
export { Reveal } from "./motion";
